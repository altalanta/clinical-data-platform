version: 2
models:
  - name: stg_subjects
    columns:
      - name: subject_id
        tests: [not_null, unique]
  - name: subject_mart
    columns:
      - name: subject_id
        tests: [not_null]
